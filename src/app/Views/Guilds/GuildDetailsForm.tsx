import React, { useState } from 'react';
import {
  View,
  Text,
  TextInput,
  Image,
  StyleSheet,
  TouchableOpacity,
  ToastAndroid,
} from 'react-native';

const GuildDetailsForm: React.FC = () => {
return <View/>;
}
export default GuildDetailsForm;
